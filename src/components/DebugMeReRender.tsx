/**
 * Epic 2.3: Build first "Debug Me" demo (e.g. re-renders).
 * Two counters (one React.memo, one not); show re-render count via useRef.
 * Add "Fix" toggle to switch to memoized version. Mount in HomePage when persona.features.debugMe.
 */
export function DebugMeReRender() {
  return (
    <div className="text-sm text-muted-foreground">
      [You implement: re-render demo with memo vs no-memo and render count]
    </div>
  )
}

/**
 * Epic 2.1: Implement CLI bar and command parser.
 * Commands: /about, /projects, /stack, /hire, /welcome. Parse on Enter, render output.
 * Style with font-mono; use theme/developer.css tokens. Wire in AppShell when persona.features.cli.
 */
export function CLIBar() {
  return (
    <div className="font-mono text-sm text-muted-foreground">
      [You implement: CLI input + output area and command handlers]
    </div>
  )
}
